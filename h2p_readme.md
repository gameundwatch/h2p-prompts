# html-to-project

動いている **HTMLプロトタイプ1枚** を、設計意図を保ったまま、そのまま開発を
始められる **フロントエンド（必要ならフルスタック）プロジェクト** へ移行する
ためのプロンプト集です。

Claude Code のようなエージェント環境に読み込ませて使います。ツール本体は
プロンプト（Markdownファイル）の集まりで、特別なインストールは要りません。

---

## これは何をするものか

1枚のHTMLファイルは、手軽に動くプロトタイプを作れる一方、いざ本格開発に
移そうとすると、コンポーネント分割・状態管理・ビルド環境・フロント/バックの
分離などが未整理のままで、手を入れにくくなります。

このツールは、その移行を **9つの段階（Phase）** に分けて、エージェントと
あなたの対話で進めます。いきなりコードを書き換えるのではなく、

- まずHTMLを **観測**し、
- 「そもそも何を作ろうとしていたか」を **言語化**し、
- 構造を **図**にして合意し、
- 動くHTMLのまま **設計意図に沿って整え**、
- スコープに合った **技術を選び**（不要なものは入れない）、
- **開発環境を立ち上げて実装**し、
- **ドキュメント**にまとめて閉じる

という流れをたどります。各段階の節目で「まだ同じように動くか」を必ず確認する
ので、**理想を盛り込んで動かなくなる**ことを防げます。

### 大事な前提
- 対象は **すでに動いているHTML**です。動かないものを直す道具ではありません。
- 進むタイミングを決めるのは **あなた**です。エージェントは勝手に次へ進みません。
- 機能の追加や「改善」は基本しません。**今あるものを忠実に移行**します。

---

## 必要なもの

- Claude Code など、ファイルの読み書きとコマンド実行（bash）ができる
  エージェント環境。
- 移行したい **動くHTMLファイル**（関連する css / js / 画像があればそれも）。
- Node.js など、選んだ技術スタックに必要な実行環境（実装段階で使います）。

---

## 配置のしかた

作業用のフォルダを用意し、その中に **このプロンプト集** と **あなたのHTML** を
置きます。プロンプト集は `prompts/` というフォルダにまとめてください。

```
my-project/                  ← 作業フォルダ（最終的にここがプロジェクトになる）
├── prompts/                 ← このツール本体
│   ├── orchestrator.md      ←   司令塔。最初にこれを読ませる
│   ├── phase1.md            ←   各段階の指示書（phase1〜9）
│   ├── phase2.md
│   ├── phase3.md
│   ├── phase4.md
│   ├── phase5.md
│   ├── phase6.md
│   ├── phase7.md
│   ├── phase8.md
│   ├── phase9.md
│   └── templates/           ←   雛形置き場
│       └── state.md         ←     進行状態ファイルの雛形（後述）
│
├── index.html               ← あなたの動くHTML（ファイル名は何でもよい）
│
├── h2p_readme.md            ← この説明書（任意。動作には不要）
└── DESIGN.md                ← 設計仕様書（任意。動作には不要）
```

ファイルの役割は3種類あります。

- **`orchestrator.md` と `phase1.md`〜`phase9.md`** … ツールの本体。エージェントが
  読んで実行する指示書です。**この10ファイルが揃っていれば中心の動作はできます。**
- **`templates/state.md`** … 進行状態を記録するファイルの **雛形**です。これ自体は
  指示書ではなく、エージェントが起動時にこれを下敷きにして、作業用の
  `.h2p/state.md`（実際の進行記録）を新しく作ります。`prompts/templates/` の
  場所に置いてください（司令塔がこのパスを参照します）。
- **`h2p_readme.md`（この説明書）と `DESIGN.md`（設計仕様書）** … 人間が読むための
  文書で、**動作には使いません**。作業フォルダのどこに置いても、別の場所に
  保管しても構いません。

> **このツールの説明書（`h2p_readme.md`）と、完成後に生成される
> プロジェクトの `README.md`（段階9で作られる）は別物です。** 後者は移行が
> 終わったプロジェクト自身の説明書で、このファイルとは役割が異なります。

> **補足：HTMLは作業フォルダの直下に置きます。** 起動すると、エージェントが
> あなたのHTMLを `.h2p/source/` という作業用フォルダへコピーし、以降はそちらを
> 軸に作業します。元のHTMLはそのまま残るので安心してください。

---

## 起動のしかた

エージェントに、まず **司令塔プロンプト `orchestrator.md` を読ませる**だけです。
作業フォルダで以下のように伝えてください（言い回しは自由です）。

```
prompts/orchestrator.md を読んで、その指示に従ってください。
このフォルダにある index.html をプロジェクトに移行したいです。
```

エージェントは起動すると、

1. 作業状態を記録する `.h2p/state.md` を作り（初回のみ）、
2. あなたのHTMLを `.h2p/source/` へ退避し、
3. 最初の段階（Phase1：HTML分析）から対話を始めます。

あとは画面の案内に沿って対話を進めるだけです。

### 次の段階へ進むには
各段階の作業が終わると、エージェントが結果を提示します。内容を確認し、
よければ **あなたから明示的に「次へ進んで」と伝えてください**。エージェントは
あなたの指示があるまで次の段階へは進みません。各段階の成果は逐一ファイルに
記録されるので、途中で中断しても大丈夫です。

### 中断・再開について
セッションが切れたり日をまたいでも、再び `orchestrator.md` を読ませれば、
`.h2p/state.md` から **今どこまで進んだか**を読み取って続きから再開します。
長い作業になるので、無理に一度で終わらせる必要はありません。

### やり直したいとき
ある段階の結果に納得できず前の段階に戻りたい場合は、「Phase◯ に戻りたい」と
伝えてください。戻った段階より後の成果はやり直しになります（その旨エージェントが
説明します）。

---

## 進行する9つの段階

| 段階 | 何をするか | あなたの関わり方 |
|------|-----------|----------------|
| 1 | HTMLを観測して事実を洗い出す | 観測結果を確認。複数HTMLなら対象を選ぶ |
| 2 | 「何を作ろうとしていたか」を言葉にし、範囲を決める | 5W1Hの問いに答え、バックエンドの有無を決める |
| 3 | 構造・データ・状態・流れを図にする | 図が意図に合っているか確認 |
| 4 | 動くHTMLのまま、意図に沿って整える | 整える箇所の方針に合意（動作は変えない） |
| 5 | 技術を選ぶ（不要なものは入れない） | 候補が複数あるものを **あなたが選ぶ** |
| 6 | 開発の進め方・規律を決める | 規模に合った進め方に合意 |
| 7 | 開発環境を立ち上げ、フロントを実装 | 元のHTMLと同じ動きか確認 |
| 8 | （必要時）バックエンドのモックを作り結合 | フロントと繋がって動くか確認 |
| 9 | ドキュメント化して仕上げる | 完成と片付けに合意 |

> 段階5の技術選びでは、エージェントが「この機能が要るか」までは判断しますが、
> 「どのライブラリにするか」候補が複数あるものは **あなたの好みや慣れ**に
> 合わせて選んでもらいます。あなたが保守していくものだからです。

> 段階8は、バックエンドが必要だと段階2で決めた場合だけ実行されます。フロント
> だけで完結するなら飛ばします。

---

## 完成すると

作業フォルダの直下に、そのまま開発を始められるプロジェクトが残ります。

```
my-project/
├── CLAUDE.md          ← プロジェクトの指針（今後の開発でエージェントが従う）
├── README.md          ← プロジェクトの説明・起動手順
├── documents/         ← 設計の記録（要件・図・技術選定の理由）
├── frontend/          ← フロントエンド一式
├── backend/  shared/  ← バックエンドが必要だった場合のみ
└── .h2p-archive/      ← このツールの作業記録（片付け後）
```

開発環境（開発サーバー）が起動する状態まで仕上がっているので、すぐに開発を
続けられます。

### 片付けについて
移行が終わると、ツール本体（`prompts/`）と作業フォルダ（`.h2p/`）は
`.h2p-archive/` へまとめて片付けられます。`.h2p/` には移行中の判断の記録が
残っているので、**消すかどうかは最後にあなたが選びます**（勝手には消しません）。

---

## つまずいたら

- **エージェントが勝手に進む / 進まない** … 進行はあなたの明示指示が合図です。
  「次へ進んで」「まだ確認したい」とはっきり伝えてください。
- **今どこにいるか分からなくなった** … `.h2p/state.md` を開けば現在地と
  これまでの決定が読めます。エージェントに「今の状態を教えて」と聞いても
  構いません。
- **動かなくなった気がする** … 各実装段階の出口で動作確認をする設計です。
  確認が通らないうちは次へ進めないので、その段階で一緒に直してください。
- **元のHTMLを失わないか不安** … 元ファイルは触らず残ります。整える作業は
  すべてコピー（`.h2p/source/`）に対して行われます。
